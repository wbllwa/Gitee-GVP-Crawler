spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/crawler?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

# 爬虫代理
crawler:
  proxy:
    host: 192.190.10.101
    port: 3128